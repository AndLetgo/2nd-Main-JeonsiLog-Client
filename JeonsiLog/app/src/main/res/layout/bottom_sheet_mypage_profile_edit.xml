<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

    </data>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/margin_16"
            android:gravity="center">

            <Button
                android:id="@+id/btn_bottom_sheet_mypage_load_image"
                android:layout_width="match_parent"
                android:layout_height="54dp"
                android:layout_marginHorizontal="@dimen/margin_4"
                android:text="@string/btn_load_image"
                style="@style/M_medium"
                android:textColor="@color/basic_black"
                android:background="@drawable/shape_corner_round_start_btn"
                android:backgroundTint="@color/basic_white"
                android:outlineProvider="none"/>

            <Button
                android:id="@+id/btn_bottom_sheet_mypage_load_defalut"
                android:layout_width="match_parent"
                android:layout_height="54dp"
                android:layout_marginHorizontal="@dimen/margin_4"
                android:text="@string/btn_set_defalut_image"
                style="@style/M_medium"
                android:layout_marginTop="@dimen/margin_12"
                android:textColor="@color/basic_black"
                android:background="@drawable/shape_corner_round_start_btn"
                android:backgroundTint="@color/basic_white"
                android:outlineProvider="none"/>

        </LinearLayout>

</layout>